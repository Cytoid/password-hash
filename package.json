{
  "name": "password-hash",
  "version": "0.1",
  "description": "Version control for password hashing and salting.",
  "sideEffects": false,
  "license": "MIT",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "devDependencies": {
    "@types/bcrypt": "^3.0.0",
    "@types/jest": "^24.0.12",
    "@types/node": "^12.0.0",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  },
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "lint": "tslint src/**/*.ts"
  },
  "dependencies": {
    "bcrypt": "^3.0.6"
  }
}
